import { Panel } from 'primereact/panel';
import { ProductForm } from '../../shared/ProductForm/ProductForm';

export const Form = () => {
  return (
    <Panel header="Form">
      <ProductForm />
    </Panel>
  );
};
